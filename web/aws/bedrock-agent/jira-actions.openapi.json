{
  "openapi": "3.0.0",
  "info": {
    "title": "ChronoFlow Jira Actions",
    "version": "1.0.0",
    "description": "Prepare Jira ticket drafts for user review in ChronoFlow."
  },
  "paths": {
    "/create-jira-ticket": {
      "post": {
        "operationId": "create_jira_ticket",
        "summary": "Prepare a Jira ticket draft",
        "description": "Prepare a Jira ticket draft for the user to review and create in ChronoFlow. Use when the user asks to create, file, or log a Jira ticket or issue. You need a title; write the description yourself if the user does not provide one.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "title": {
                    "type": "string",
                    "description": "Short Jira issue summary."
                  },
                  "description": {
                    "type": "string",
                    "description": "Issue description. Optional — ChronoFlow generates one from the title when omitted."
                  },
                  "context": {
                    "type": "string",
                    "description": "Optional extra user context to help generate the description."
                  },
                  "priority": {
                    "type": "string",
                    "enum": ["High", "Medium", "Low"]
                  }
                },
                "required": ["title"]
              }
            }
          }
        },
        "responses": {
          "200": { "description": "Jira ticket draft prepared" },
          "400": { "description": "Missing ticket fields" },
          "500": { "description": "Ticket preparation failed" }
        }
      }
    }
  }
}
